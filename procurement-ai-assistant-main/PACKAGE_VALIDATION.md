# Package validation

All 10 skill packages passed the repository build checks:

- folder name matches the skill name
- lowercase/hyphen skill names
- descriptions kept within Claude's 200-character guidance
- `SKILL.md` exists with YAML frontmatter
- each ZIP has one skill folder at its root
- each package includes the MIT licence
